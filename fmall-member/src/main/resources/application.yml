server:
  port: 8000
spring:
  application:
    name: fmall-member
  datasource:
    username: root
    password: root
    url: jdbc:mysql://169.254.122.40:3306/fmall_ums?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: fmall.com
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  thymeleaf:
    cache: false
  session:
    store-type: redis


#配置mapper文件的位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      #设置主键自增
      id-type: auto
